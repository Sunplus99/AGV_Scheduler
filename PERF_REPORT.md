## 性能测试环境与报告

### 1. 测试环境

#### 1.1 宿主机配置

- **设备型号**：戴尔 Inspiron 16 Plus 7620
- **CPU**：英特尔 12 代酷睿 i7-12700H（14 核）
- **内存**：16GB DDR4（4800MHz）
- **操作系统**：Windows 11 专业版（64 位）
- **虚拟机软件**：VMware Workstation 17 Pro (17.5.1)

#### 1.2 虚拟机配置（服务端 / 客户端一致）

- **操作系统**：Ubuntu 20.04.6 LTS (Focal Fossa) 64 位
- **CPU**：4 核（与宿主机核心隔离）
- **内存**：4GB
- **硬盘**：40GB（SCSI）
- **网络模式**：NAT（VMnet10，子网 192.168.184.0/24）
- **IP 地址**:
  - 服务端：`192.168.184.128`
  - 客户端：`192.168.184.132`

#### 1.3 系统调优参数

```bash
# 1. 文件描述符上限（解决高并发连接数限制）
ulimit -n 65535

# 2. 扩大本地端口范围（避免端口耗尽）
sudo sysctl -w net.ipv4.ip_local_port_range="1024 65535"

# 3. 关闭防火墙（确保网络连通）
sudo ufw disable
```

---

### 2. 测试目标

验证基于 Reactor 多线程模型的自定义 TCP 服务器在以下维度的性能表现：

1. **最大并发长连接数**：验证服务器对海量空闲连接的承载能力。
2. **请求延迟分布**：统计 P50/P90/P99 延迟，定位慢请求瓶颈。
3. **峰值 QPS**：多线程并发下服务器每秒可处理的请求数。
4. **大包吞吐量**：10KB 报文场景下的数据传输效率（MB/s）。

---

### 3. 测试工具说明

因服务采用**自定义 TCP 协议（4 字节消息头 + 包体）**，不适用 HTTP 压测工具（如 wrk），故基于 AI 生成模板改造了以下压测工具：

| 工具名称          | 核心功能                                                  |
| ----------------- | --------------------------------------------------------- |
| `test_concurreny` | 单进程创建 10000 TCP 长连接，测试最大并发连接数。         |
| `test_latency`    | 单连接 10000 次请求，高精度计时并统计延迟分位值。         |
| `test_qps`        | 多线程并发请求，原子计数统计 QPS，验证 Reactor 吞吐能力。 |
| `test_throughput` | 10KB 大包压测，封装 `readn()` 处理 TCP 分包，计算吞吐量。 |

---

### 4. Reactor测试结果

#### 4.1 最大并发连接数 (`sudo ./test_concurrency`)

- **测试条件**：单进程循环创建连接，无业务请求，仅保持连接存活。
- **实测结果**：成功建立 **10000** 个长连接，服务器无崩溃、无连接中断。
- **关键结论**：文件描述符与端口范围调优后，服务器可稳定承载上万级空闲连接。

#### 4.2 请求延迟分布（单连接，64B 报文）

|   指标   | 数值 (µs) |
| :------: | :-------: |
| 总请求数 |   10000   |
|   Min    |  319.24   |
|   Avg    |  934.89   |
|   P50    |  828.83   |
|   P90    |  1318.18  |
|   P95    |  1755.18  |
|   P99    |  2929.75  |
|   Max    |  5852.12  |

**关键结论**：单连接 64B 报文场景下，10000 次请求无失败，P99 延迟为 2929.75μs，最大延迟降至 5.85ms，整体延迟分布紧凑无大规模长尾请求；延迟数值偏高系虚拟机虚拟化开销导致，验证了 Reactor 模型的协议解析与 IO 调度逻辑稳定、无异常。

#### 4.3 峰值 QPS（100 线程并发，64B 报文）

- **测试条件**：100 个客户端线程，持续 Ping-Pong 请求。
- **实测结果**：32 秒累计处理 120.7 万次请求，平均 QPS≈3.8 万，峰值 QPS 达 4.5 万；服务端无请求失败、无连接中断; 服务端进程 CPU 利用率约 220%（4 核总算力的 55%）。
- **关键结论**：Reactor 模型的 IO 线程与工作线程分离设计，保障了高并发下的吞吐稳定性，全程无性能断崖、无请求失败。

#### 4.4 大包吞吐量（10KB 报文，20 线程）

- **测试条件**：10KB 报文，20 线程并发，双向传输。
- **实测结果**：平均 QPS≈6666 req/sec，平均吞吐量≈130.20 MB/s；峰值 QPS 达 7322 req/sec，峰值吞吐量 143.01 MB/s；全程无请求失败、无连接中断。
- **关键结论**：大数据包场景下，服务器吞吐稳定，验证了 Reactor 模型在大流量场景下的可靠性。

---

### 5. AGV调度服务器测试结果

#### 5.1 测试场景（基准场景）

基于 Reactor 网络层验证通过的基础上，聚焦 AGV 调度核心流程验证，固定以下基准场景参数：

| 场景维度      | 配置值                                               |
| ------------- | ---------------------------------------------------- |
| 地图规格      | 100×100 网格，障碍物比例 20%（随机生成）             |
| 模拟 AGV 数量 | 30 台（客户端虚拟机独立模拟）                        |
| 任务下发规则  | 任务总数 = 2 × AGV 数量（共 60 个）                  |
| 通信协议      | 自定义 TCP 协议（4 字节消息头 + JSON 包体）          |
| 核心算法      | A * 路径规划、贪心任务调度（无算法创新，仅验证流程） |
| 统计方式      | 仅统计算法纯计算时间（排除网络 / IO 开销）           |

#### 5.2 核心性能指标

| 指标分类         | 总请求数 | 平均延迟 (ms) | P50 延迟 (ms) | P95 延迟 (ms) | P99 延迟 (ms) |
| ---------------- | -------- | ------------- | ------------- | ------------- | ------------- |
| 贪心调度延迟     | 104      | 0.05          | 0.04          | 0.09          | 0.12          |
| A * 路径规划延迟 | 241      | 0.10          | 0.07          | 0.27          | 0.88          |

#### 5.3 关键结论

1. 核心流程验证：AGV 调度服务器可完整跑通 “地图生成→AGV 连接→任务下发→路径规划→任务调度” 全流程，30 台 AGV 无连接中断、任务丢失问题；
2. 性能表现：算法纯计算延迟均处于亚毫秒级，无长尾请求（P99 延迟≤0.88ms），验证基础调度 / 寻路逻辑高效、稳定；
3. 场景适配性：因地图随机生成、任务数与 AGV 数量强绑定，基准场景下性能稳定即可”满足流程跑通、基础性能达标“的验证目标，故未开展额外多场景压测。
